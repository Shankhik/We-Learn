*-- Handling Exception --*{

    const [users, setUsers] = useState < User_mongo[]> ([]);

    return(
        <div>
            {users.length>0 ? users[0].username: ''}
        </div>
    )
}

*-- Creating Error --*{
    //Error has type ErrorProps
    throw new Error({
        message: 'No users field found!',
        statusCode: 500
    })
}